# Superseded trial outputs

These four researcher submissions were produced before the campaign degree regime was corrected to
\(100<d<p\). They concentrated on the now-out-of-scope endpoint \(d=0\), so they were removed from
the active leaderboard and preserved here only as provenance. Their original invocation traces remain
under `agent_logs/`. They must not be used by researchers, verifiers, or GENIUS as candidate progress
for the corrected campaign.
